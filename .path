# Pyenv
export PYENV_ROOT="$HOME/.pyenv"

# Add ~/bin to $PATH
export PATH="$PYENV_ROOT/bin:$HOME/bin:/usr/local/bin:/usr/local/opt/openssl/bin:$PATH";

if command -v pyenv 1>/dev/null 2>&1; then
    eval "$(pyenv init -)"
#    eval "$(pyenv virtualenv-init -)"
fi
